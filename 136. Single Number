class Solution {
    public int singleNumber(int[] nums) {
        int result = 0;
        for (int num : nums) {
            // Use XOR to cancel out pairs
            result ^= num;
        }
        return result;
    }
}
